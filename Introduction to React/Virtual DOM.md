# Virtual DOM

- It is a lightweight in-memory virtual representation of the DOM tree

- It abstracts away direct DOM manipulations and makes efficient DOM operations

- It batches operations and makes *only the minimum necessary changes*

- When a corresponding state changes, React reflects that change in the UI
