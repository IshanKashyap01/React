# Unmounting Phase Methods

## `componentWillUnmount()`

- The only method of this phase, gets invoked right before the component is
unmounted

- It is used to perform cleanup operations such as closing intervals/timeouts,
removing event listeners, cancelling network requests and closing subscriptions
