# Custom Hooks

- They are used when you require complicated side-effects or state management
logic and/or you have duplicate hooks among components

- A custom hook's name must start with `use` and followed by a *capital letter*

- Because they are custom, you control what they input and output

```js
function useLocalStorage()
{
    
}
```
